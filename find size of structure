#include <stdio.h>

struct Student {
    char name[50];
    int age;
    float grade;
};

int main() {
    printf("Size of the structure Student: %zu bytes\n", sizeof(struct Student));

    return 0;
}
